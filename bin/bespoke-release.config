%% -*- erlang -*-
[{webapp,
  [{http_port, 80},
   {https_port, 443}]},
 {sasl, [{sasl_error_logger, false},
         {errlog_type, all}]},
 {kernel,
  [{logger_level, debug}]},
 {logger,
  [{level, debug},
   {handlers, [{default, logger_std_h,
                #{config => #{file => "/var/tmp/bespoke.log"},
                  filter_default => log}}]}]}].
